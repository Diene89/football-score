export interface IUser {
    email: string,
    password: string,
}

// import ICreateToken from '../interfaces/ICreateToken'

// export interface IUser {
//     create({ email, password }: any): Promise<ICreateToken>
//   }